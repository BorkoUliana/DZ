def delite():
    """
    Функция для удаления всех значений равных второму переданному числу
    
    Возвращает: 
    spisok: список, из котором удалены все значения равные второму числу
    """
    # Ввод списка чисел
    spisok = list(map(int,input("Введите чила через пробел").split()))
    #Ввод числа для удаления
    num = int(input("Введите число, которое хотите удалить"))
    return ([a for a in spisok if a!=num])
    print(delite())